- content_for :title, "Tag: #{tagname}"

%section
  %h1 Articles tagged '#{tagname}'

  %ul.category-list
    - page_articles.each_with_index do |article, i|
      - if article.published?
        %li
          = link_to article.title, article

